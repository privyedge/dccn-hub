apiVersion: v1
kind: Service
metadata:
  name: api-listener-service
  labels:
    name: api-listener-service
spec:
  type: LoadBalancer
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
  selector:
    name: api-listener