apiVersion: v1
kind: Service
metadata:
  name: k8s-adapter-service
  labels:
    name: k8s-adapter-service
spec:
  type: LoadBalancer
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
  selector:
    name: k8s-adapter