syntax = "proto3";

package go.micro.srv.v1.mail;

service Mail {
	rpc Send(MailEvent) returns (Response) {}
}

message MailEvent {
	string name = 1;
	string address = 2;
	string message = 3;
}


message Response {
}